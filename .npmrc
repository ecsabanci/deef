# Expo/Metro in a pnpm monorepo needs npm-style hoisted node_modules —
# with the default isolated linker, Metro can resolve two React copies
# (mobile's pinned 19.1.0 + backend's ^19.1.0 resolution), which breaks
# all hooks ("Invalid hook call"). Official Expo guidance for pnpm.
node-linker=hoisted
